# Hostinger Deployment Environment Variables
# Copy this file and fill in the values before deploying

# Database password for PostgreSQL
DB_PASSWORD=your-secure-database-password

# NextAuth secret - generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key

# OpenRouter API key for dream interpretation
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# ElevenLabs API key for voice transcription (Speech-to-Text)
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# VAPID keys for push notifications - generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# CRON secret for protected API endpoints - generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-key
